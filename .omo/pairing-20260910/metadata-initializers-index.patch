diff --git a/clients/rust/erd-app/src/bin/erd_client.rs b/clients/rust/erd-app/src/bin/erd_client.rs
index a111afa..81e34d4 100644
--- a/clients/rust/erd-app/src/bin/erd_client.rs
+++ b/clients/rust/erd-app/src/bin/erd_client.rs
@@ -461,6 +461,8 @@ fn run_client(mut cli: Cli) -> Result<()> {
                 name: cli.host.clone(),
                 key,
                 added_at_unix_ms: 0,
+                last_endpoint: None,
+                endpoint_aliases: Vec::new(),
             };
             info!(pairing_id = %pairing.id, "Connecting with explicit PSK");
             session
diff --git a/clients/rust/erd-app/src/session.rs b/clients/rust/erd-app/src/session.rs
index e6b06be..2de599d 100644
--- a/clients/rust/erd-app/src/session.rs
+++ b/clients/rust/erd-app/src/session.rs
@@ -266,6 +275,8 @@ impl ClientSession {
                     name: grant.host_name,
                     key: grant.key.to_vec(),
                     added_at_unix_ms: current_unix_ms(),
+                    last_endpoint: None,
+                    endpoint_aliases: Vec::new(),
                 };
                 self.store.save(record.clone())?;
                 record
@@ -1387,6 +1461,8 @@ mod cancellation_tests {
             name: "mock-host".to_string(),
             key: key.to_vec(),
             added_at_unix_ms: 0,
+            last_endpoint: None,
+            endpoint_aliases: Vec::new(),
         };
         let ready = session.connect_with_pairing(record).unwrap();
         assert_eq!(ready.server.name, "mock-host");
diff --git a/clients/rust/erd-app/src/tcp_write_tests.rs b/clients/rust/erd-app/src/tcp_write_tests.rs
index 6f52ca2..3ee9179 100644
--- a/clients/rust/erd-app/src/tcp_write_tests.rs
+++ b/clients/rust/erd-app/src/tcp_write_tests.rs
@@ -70,6 +70,8 @@ fn fixture() -> Fixture {
             name: "peer".into(),
             key: vec![19; 32],
             added_at_unix_ms: 0,
+            last_endpoint: None,
+            endpoint_aliases: Vec::new(),
         })
         .unwrap();
     let peer = peer_rx.recv_timeout(WATCHDOG).unwrap();
diff --git a/clients/rust/erd-app/tests/receiver_telemetry.rs b/clients/rust/erd-app/tests/receiver_telemetry.rs
index cdd820c..aa3b829 100644
--- a/clients/rust/erd-app/tests/receiver_telemetry.rs
+++ b/clients/rust/erd-app/tests/receiver_telemetry.rs
@@ -83,6 +83,8 @@ impl Connected {
                 name: "fixture".into(),
                 key: key.to_vec(),
                 added_at_unix_ms: 0,
+                last_endpoint: None,
+                endpoint_aliases: Vec::new(),
             })
             .unwrap();
         session
diff --git a/clients/rust/erd-app/tests/session_mock.rs b/clients/rust/erd-app/tests/session_mock.rs
index 61d2347..beec0f4 100644
--- a/clients/rust/erd-app/tests/session_mock.rs
+++ b/clients/rust/erd-app/tests/session_mock.rs
@@ -235,6 +235,8 @@ fn connect_with_pairing_direct_round_trip() {
         name: "mock-host".to_string(),
         key: key.to_vec(),
         added_at_unix_ms: 0,
+        last_endpoint: None,
+        endpoint_aliases: Vec::new(),
     };
     let ready = session.connect_with_pairing(record).unwrap();
     assert_eq!(ready.server.name, "mock-host");
@@ -330,6 +332,8 @@ fn stalled_consumer_retains_latest_clipboard_and_terminal_error() {
             name: "host".into(),
             key: key.to_vec(),
             added_at_unix_ms: 0,
+            last_endpoint: None,
+            endpoint_aliases: Vec::new(),
         })
         .unwrap();
     let mut runtime = session.spawn_tcp_runtime().unwrap();
@@ -421,6 +425,8 @@ fn mock_server_abr_bitrate_adjust_round_trip() {
             name: "host".into(),
             key: key.to_vec(),
             added_at_unix_ms: 0,
+            last_endpoint: None,
+            endpoint_aliases: Vec::new(),
         })
         .unwrap();
 
@@ -503,6 +509,8 @@ fn mock_server_stream_config_negotiation_round_trip() {
             name: "host".into(),
             key: key.to_vec(),
             added_at_unix_ms: 0,
+            last_endpoint: None,
+            endpoint_aliases: Vec::new(),
         })
         .unwrap();
 
@@ -585,6 +593,8 @@ fn udp_client_rejects_host_missing_authenticated_registration_capability() {
         name: "mock-host".to_string(),
         key: key.to_vec(),
         added_at_unix_ms: 0,
+        last_endpoint: None,
+        endpoint_aliases: Vec::new(),
     };
     let result = session.connect_with_pairing(record);
     assert!(
@@ -677,6 +687,8 @@ fn udp_client_sends_authenticated_registration_and_preserves_nonce() {
         name: "mock-host".to_string(),
         key: key.to_vec(),
         added_at_unix_ms: 0,
+        last_endpoint: None,
+        endpoint_aliases: Vec::new(),
     };
     let ready = session.connect_with_pairing(record).unwrap();
     assert_eq!(ready.server.name, "mock-host");
diff --git a/clients/rust/erd-host/tests/pairing_isolation.rs b/clients/rust/erd-host/tests/pairing_isolation.rs
index 6904626..74b7a1f 100644
--- a/clients/rust/erd-host/tests/pairing_isolation.rs
+++ b/clients/rust/erd-host/tests/pairing_isolation.rs
@@ -48,6 +48,8 @@ fn test_outbound_client_pairing_rejected_as_host_authorization() {
             name: "RemoteHostName".into(),
             key: client_key.clone(),
             added_at_unix_ms: 1700000000000,
+            last_endpoint: None,
+            endpoint_aliases: Vec::new(),
         };
         client_store.save(client_record.clone()).unwrap();
 
diff --git a/clients/rust/erd-mobile/src/storage.rs b/clients/rust/erd-mobile/src/storage.rs
index 8e046c8..c25d6c5 100644
--- a/clients/rust/erd-mobile/src/storage.rs
+++ b/clients/rust/erd-mobile/src/storage.rs
@@ -489,6 +489,8 @@ mod tests {
             name: "Galaxy S24".into(),
             key: vec![0x42; 32],
             added_at_unix_ms: 1700000000000,
+            last_endpoint: None,
+            endpoint_aliases: Vec::new(),
         };
 
         store.save_record(&record).unwrap();
@@ -514,6 +516,8 @@ mod tests {
             name: "Workstation".into(),
             key: vec![0x33; 32],
             added_at_unix_ms: 1700000000000,
+            last_endpoint: None,
+            endpoint_aliases: Vec::new(),
         };
         store.save_record(&record).unwrap();
 
diff --git a/clients/rust/ios-shell/src/tests.rs b/clients/rust/ios-shell/src/tests.rs
index 914f942..6262fbb 100644
--- a/clients/rust/ios-shell/src/tests.rs
+++ b/clients/rust/ios-shell/src/tests.rs
@@ -319,6 +317,8 @@ mod tests {
             name: "Workstation".to_string(),
             key: vec![0x42; 32],
             added_at_unix_ms: 1000,
+            last_endpoint: None,
+            endpoint_aliases: Vec::new(),
         };
         store.save(paired_record).unwrap();
 
diff --git a/clients/rust/tauri-shell/src-tauri/src/desktop_integration_tests.rs b/clients/rust/tauri-shell/src-tauri/src/desktop_integration_tests.rs
index c5bedae..039c03c 100644
--- a/clients/rust/tauri-shell/src-tauri/src/desktop_integration_tests.rs
+++ b/clients/rust/tauri-shell/src-tauri/src/desktop_integration_tests.rs
@@ -514,12 +514,16 @@ fn pairing_store_round_trip_and_deletion() {
         name: "host-1".into(),
         key: vec![1; 32],
         added_at_unix_ms: 1000,
+        last_endpoint: None,
+        endpoint_aliases: Vec::new(),
     };
     let record2 = erd_app::PairingRecord {
         id: "id-2".into(),
         name: "host-2".into(),
         key: vec![2; 32],
         added_at_unix_ms: 2000,
+        last_endpoint: None,
+        endpoint_aliases: Vec::new(),
     };
     store.save(record1.clone()).unwrap();
     store.save(record2.clone()).unwrap();
diff --git a/clients/rust/tauri-shell/src-tauri/src/discovery_tests.rs b/clients/rust/tauri-shell/src-tauri/src/discovery_tests.rs
index 2a5f4bc..f8139f8 100644
--- a/clients/rust/tauri-shell/src-tauri/src/discovery_tests.rs
+++ b/clients/rust/tauri-shell/src-tauri/src/discovery_tests.rs
@@ -68,6 +68,8 @@ fn record(id: &str, name: &str) -> erd_app::PairingRecord {
         name: name.into(),
         key: vec![0; 32],
         added_at_unix_ms: 0,
+        last_endpoint: None,
+        endpoint_aliases: Vec::new(),
     }
 }
 
diff --git a/clients/rust/tauri-shell/src-tauri/src/mailbox_tests.rs b/clients/rust/tauri-shell/src-tauri/src/mailbox_tests.rs
index 3219373..d5b18cc 100644
--- a/clients/rust/tauri-shell/src-tauri/src/mailbox_tests.rs
+++ b/clients/rust/tauri-shell/src-tauri/src/mailbox_tests.rs
@@ -79,6 +79,8 @@ impl Connected {
                 name: "host".into(),
                 key: key.to_vec(),
                 added_at_unix_ms: 0,
+                last_endpoint: None,
+                endpoint_aliases: Vec::new(),
             })
             .unwrap();
         let runtime = session.spawn_tcp_runtime().unwrap();
